basic functionality:
* blocks webpage from loading
* somehow blocks existing webpage (if matches url).

This probably requires like activeTab. Should I redirect it? could just delete the tab.

then maybe use the popup to toggle on or off (or add/remove websites from block page).